* 유클리드 호제법 - 최대 공약수 구하는 방법

  - 큰 숫자 % 작은 숫자 == 0이면, 작은 숫자가 최대 공약수
  - 0이 아닐 경우, 작은 숫자와 위에서 계산한 나머지와 나눔
  - 나머지가 0이 될 때까지 위의 사항 반복
  
  - 반복문이 아닌, 재귀 형태로 구현
    # public int num(int a, int b){
        if(a%b == 0)
          return b;
        return num(b, a%b);
      }
