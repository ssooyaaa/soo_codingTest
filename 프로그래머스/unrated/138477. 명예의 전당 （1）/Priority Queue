* Priority Queue

  - 선언
    # PriorityQueue<> pq = new PriorityQueue<>(); : 우선순위가 낮은 숫자 순
    # PriorityQueue<> pq = new PriorityQueue<>(Collections.reverseOrder()); : 우선순위가 높은 숫자 순
    
  - 값 추가
    # pq.add();
    # pq.offer();
    
  - 값 삭제
    # pq.poll(); : 첫번째 값 반환 후, 제거
    # pq.remove(); : 첫번째 값 제거
    # pq.clear(); : 초기화
    
  - 우선순위가 가장 높은 값 출력
    # pq.peek(); : 첫번째 값 참조
